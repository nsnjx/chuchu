# Version configuration for Android builds
# This file can be modified before building to set custom version
# Priority: version.properties > local.properties > environment variables > default

versionName=0.1.0
versionCode=5

